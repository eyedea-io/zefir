{
  "scripts": {
    "lint": "standard --verbose | snazzy",
    "lint:fix": "standard --fix",
    "release": "lerna publish --conventional-commits --npm-tag=beta"
  },
  "standard": {
    "parser": "babel-eslint",
    "ignore": [
      "**/dist/",
      "**/node_modules/**"
    ]
  },
  "dependencies": {
    "lerna": "2.0.0-beta.38"
  },
  "devDependencies": {
    "standard": "^10.0.0-beta.2",
    "babel-eslint": "^7.1.1",
    "snazzy": "^6.0.0"
  }
}
